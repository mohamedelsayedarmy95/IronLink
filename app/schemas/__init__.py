"""Pydantic v2 request/response models for the newer feature areas.

The original API surface keeps its schemas inline in ``app.api.schemas``; the
channel / community / creator features carry enough shapes to warrant their own
package.
"""
